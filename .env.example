# Copy to .env and adjust as needed. Local Postgres runs via `docker compose up -d`.
DATABASE_URL="postgresql://household:household@127.0.0.1:5432/household"

# Generate with: openssl rand -base64 32
AUTH_SECRET="change-me"

# Not used yet (wired up in Milestone 2 alongside calendar sync) — left here so the
# shape of required config is visible from day one.
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# MICROSOFT_CLIENT_ID=
# MICROSOFT_CLIENT_SECRET=
# PUSHER_APP_ID=
# PUSHER_KEY=
# PUSHER_SECRET=
